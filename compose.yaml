services:
  pi:
    image: ghcr.io/phate334/uv-pi:0.80.3
    working_dir: /workspace
    volumes:
      # Agent state and cache.
      - uv-pi-cache:/root/.cache/uv
      - ./.pi:/root/.pi

      # Read-only project instructions and runtime files.
      - ./AGENTS.md:/workspace/AGENTS.md:ro
      - ./.agents/skills:/workspace/.agents/skills:ro
      - ./analyze_results.py:/workspace/analyze_results.py:ro
      - ./pyproject.toml:/workspace/pyproject.toml:ro
      - ./uv.lock:/workspace/uv.lock:ro

      # Writable experiment inputs/outputs.
      - ./trading_strategy.py:/workspace/trading_strategy.py
      - ./data:/workspace/data

    command:
      - pi
      - --provider
      - custom-openai
      - --model
      - gemma-4-31b
      - --print
      - 依照 AGENTS.md 的指示，分析既有實驗結果並寫入 ./data/next_params.json。

volumes:
  uv-pi-cache:
